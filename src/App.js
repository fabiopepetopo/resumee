import { Layout } from "./components";

export const App = () => {
  return (
    <div
      className="bg-no-repeat"
      style={{ backgroundImage: `url('images/waves3.svg')` }}
    >
      <Layout />;
    </div>
  );
};
